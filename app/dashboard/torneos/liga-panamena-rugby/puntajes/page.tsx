'use client'

import { useState } from 'react'
import { Award, CalendarDays, Check, ChevronDown, TrendingUp } from 'lucide-react'
import { categorias } from '../../../../../simulaDatos'
import TournamentHeader from '../../../../../Components/tournament-header'

const results = [
  ['Fecha 1', '1ra División', 'Titanes', 'Cuervos', '24 - 18'],
  ['Fecha 1', '2da División', 'Guerreros', 'Power Clan', '19 - 12'],
  ['Fecha 1', 'Femenina', 'Lycans', 'Targarens', '15 - 15'],
  ['Fecha 2', '1ra División', 'Cuervos', 'Centauros', '21 - 14'],
]

const calendarDates: Record<string, string> = {
  'Fecha 1': 'jueves, 2 de mayo de 2026',
  'Fecha 2': 'jueves, 9 de mayo de 2026',
  'Fecha 3': 'jueves, 16 de mayo de 2026',
}

const leaders = [
  ['Máximo anotador', 'Titanes', '86 puntos', 'bg-[#e9fbd0]'],
  ['Más tries', 'Edwin Jimenez', '9 tries', 'bg-[#eef4ff]'],
  ['Mejor defensa', 'Cuervos', '58 puntos recibidos', 'bg-[#fff5df]'],
]

const teamLogos: Record<string, string> = { Titanes: '/equipos/Titanes.png', Cuervos: '/equipos/cuervos.png' }

export default function ScoresPage() {
  const [categoryId, setCategoryId] = useState(categorias[0]?.id ?? '')
  const [dateFilter, setDateFilter] = useState('Fecha 1')
  const [dateMenuOpen, setDateMenuOpen] = useState(false)
  const category = categorias.find((item) => item.id === categoryId) ?? categorias[0]
  const visibleResults = dateFilter === 'Todas las fechas' ? results : results.filter((result) => result[0] === dateFilter)
  const selectedDateLabel = dateFilter === 'Todas las fechas' ? 'Todas las fechas' : `${dateFilter} · ${calendarDates[dateFilter]}`

  return <main className="min-h-screen bg-[#f4f6f8] px-5 py-8 text-[#17212b] lg:pl-72 lg:pr-10"><div className="mx-auto max-w-7xl"><TournamentHeader activeSection="Puntajes" /><section className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-start"><div><p className="font-heading text-sm uppercase tracking-[.2em] text-[#70b719]">Seguimiento de la competencia</p><h1 className="font-display text-4xl uppercase sm:text-5xl">Puntajes</h1><p className="mt-1 text-slate-500">Consulta la clasificación, resultados recientes y líderes de la liga.</p></div><div className="relative w-full sm:w-80"><button type="button" onClick={() => setDateMenuOpen((open) => !open)} className="flex w-full items-center justify-between rounded-xl border border-[#70b719] bg-white px-4 py-3 text-left text-sm font-semibold text-[#243650] shadow-sm"><span className="truncate"><CalendarDays size={17} className="mr-2 inline text-[#081522]" />{selectedDateLabel}</span><ChevronDown size={18} className={`shrink-0 text-[#081522] transition ${dateMenuOpen ? "rotate-180" : ""}`} /></button>{dateMenuOpen && <div className="absolute right-0 z-20 mt-2 w-full rounded-xl border border-slate-200 bg-white p-2 shadow-xl"><button type="button" onClick={() => { setDateFilter("Todas las fechas"); setDateMenuOpen(false) }} className={`flex w-full items-center gap-3 rounded-lg px-3 py-3 text-left text-sm ${dateFilter === "Todas las fechas" ? "bg-[#f0f8e8] font-semibold text-[#4c8500]" : "text-slate-600 hover:bg-slate-50"}`}><CalendarDays size={17} />Todas las fechas{dateFilter === "Todas las fechas" && <Check size={17} className="ml-auto" />}</button>{Object.entries(calendarDates).map(([date, calendarDate]) => <button key={date} type="button" onClick={() => { setDateFilter(date); setDateMenuOpen(false) }} className={`flex w-full items-start gap-3 rounded-lg px-3 py-3 text-left text-sm ${dateFilter === date ? "bg-[#f0f8e8] font-semibold text-[#4c8500]" : "text-slate-600 hover:bg-slate-50"}`}><CalendarDays size={17} className="mt-0.5 shrink-0" /><span>{date} · {calendarDate}</span>{dateFilter === date && <Check size={17} className="ml-auto mt-0.5 shrink-0" />}</button>)}</div>}</div></div></section><section className="mt-6 grid gap-6 xl:grid-cols-[minmax(0,1.4fr)_minmax(300px,.8fr)]"><article className="min-w-0 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div><h2 className="font-display text-3xl uppercase">Tabla de posiciones</h2><p className="mt-1 text-sm text-slate-500">Clasificación actual de la competencia.</p></div><div className="mt-5 flex gap-2 overflow-x-auto pb-1">{categorias.map((item) => <button key={item.id} type="button" onClick={() => setCategoryId(item.id)} className={`whitespace-nowrap rounded-lg px-4 py-2 text-sm font-bold ${category.id === item.id ? 'bg-[#081522] text-white' : 'bg-slate-100 text-slate-500 hover:bg-[#e9fbd0]'}`}>{item.nombre}</button>)}</div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[680px] border-collapse text-sm"><thead className="border-b border-slate-200 text-xs uppercase text-slate-500"><tr>{['Pos.', 'Equipo', 'PTS', 'PJ', 'G', 'E', 'P', '+PT', '-PT', 'DIF'].map((heading, index) => <th key={heading} className={`px-3 py-3 ${index > 1 ? 'text-center' : 'text-left'} ${heading === 'PTS' ? 'bg-[#e9fbd0] text-[#17212b]' : ''}`}>{heading}</th>)}</tr></thead><tbody>{category.tabla.map((team, index) => <tr key={team.equipo} className="border-b border-slate-100 last:border-0"><td className="px-3 py-4 font-bold text-[#70b719]">{index + 1}</td><td className="px-3 py-4 font-semibold">{team.equipo}</td><td className="bg-[#f0f8e8] px-3 py-4 text-center text-lg font-black text-[#4c8500]">{team.puntos}</td><td className="px-3 py-4 text-center text-slate-500">{team.pj}</td><td className="px-3 py-4 text-center text-slate-500">{team.ganados}</td><td className="px-3 py-4 text-center text-slate-500">{team.empatados}</td><td className="px-3 py-4 text-center text-slate-500">{team.perdidos}</td><td className="px-3 py-4 text-center text-slate-500">{team.aFavor}</td><td className="px-3 py-4 text-center text-slate-500">{team.enContra}</td><td className="px-3 py-4 text-center font-bold">{team.aFavor - team.enContra}</td></tr>)}</tbody></table></div></article><article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-start justify-between"><div><h2 className="font-display text-3xl uppercase">Líderes</h2><p className="mt-1 text-sm text-slate-500">Destacados del torneo.</p></div><Award className="text-[#70b719]" /></div><div className="mt-5 space-y-3">{leaders.map(([title, name, value, color]) => <div key={title} className={`rounded-xl p-4 ${color}`}><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{title}</p><p className="mt-1 text-lg font-bold">{name}</p><p className="text-sm text-slate-600">{value}</p></div>)}</div></article></section><section className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><div><h2 className="font-display text-3xl uppercase">Resultados recientes</h2><p className="mt-1 text-sm text-slate-500">Partidos finalizados por fecha.</p></div><TrendingUp className="text-[#70b719]" /></div><div className="mt-5 space-y-3">{visibleResults.map(([date, division, home, away, score]) => <div key={`${date}-${home}-${away}`} className="grid gap-4 rounded-2xl border border-slate-200 p-4 sm:grid-cols-[150px_minmax(115px,.7fr)_minmax(0,1.8fr)_110px_auto] sm:items-center sm:p-5"><div className="text-sm font-bold"><span className="flex items-center gap-2"><CalendarDays size={18} className="text-[#4cba17]" />{date}</span><span className="mt-1 block pl-7 text-xs font-normal leading-5 text-slate-500">{calendarDates[date]}</span></div><span className="w-fit rounded-full bg-[#e9fbd0] px-4 py-2 text-sm font-bold text-[#4c8500]">{division}</span><div className="grid grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)] items-center gap-2 text-center"><div className="min-w-0"><ResultLogo name={home} /><p className="truncate font-bold">{home}</p></div><span className="text-sm font-bold text-slate-400">VS.</span><div className="min-w-0"><ResultLogo name={away} /><p className="truncate font-bold">{away}</p></div></div><strong className="rounded-xl bg-[#eef8ed] px-4 py-3 text-center text-2xl text-[#164b3d]">{score}</strong><span className="hidden text-2xl text-slate-400 sm:block">›</span></div>)}</div>{visibleResults.length === 0 && <p className="py-8 text-center text-slate-500">No hay resultados para esta fecha.</p>}</section></div></main>
}

function ResultLogo({ name }: { name: string }) { const src = teamLogos[name]; return src ? <img src={src} alt={`Logo de ${name}`} className="mx-auto mb-1 h-10 w-10 object-contain" /> : <span className="mx-auto mb-1 flex h-10 w-10 items-center justify-center rounded-full bg-[#081522] text-sm font-bold text-[#B4FF45]">{name.slice(0, 2).toUpperCase()}</span> }
